<%# we do this trick because some html elements should use 'label' and some should be plain paragraphs %>
<% if @champ.html_label? %>
  <%= @form.label @champ.main_value_name, id: input_label_id(@champ), for: @champ.focusable_input_id, class: 'fr-label' do %>
    <%= render EditableChamp::ChampLabelContentComponent.new(form: @form, champ: @champ, seen_at: @seen_at, row_number: @row_number) %>
  <% end %>
  <% if @champ.description.present? %>
    <div class="fr-hint-text fr-mb-1w" id="<%= @champ.describedby_id %>">
      <%= render SimpleFormatComponent.new(@champ.description, allow_a: true) %>
    </div>
  <% end %>
  <% if @champ.type_de_champ.formatted_advanced? && @champ.type_de_champ.expression_reguliere_indications.present? %>
    <div class="fr-hint-text" id="<%= @champ.describedby_id %>">
      <%= @champ.type_de_champ.expression_reguliere_indications %>
    </div>
  <% end %>
  <% if @champ.type_de_champ.referentiel? && @champ.type_de_champ&.referentiel&.hint&.present? %>
    <div class="fr-hint-text" id="<%= @champ.describedby_id %>">
      <%= t('.referentiel', hint: @champ.type_de_champ.referentiel.hint) %>
    </div>
  <% end %>
<% elsif @champ.legend_label? %>
  <legend
    class="fr-fieldset__legend fr-text--regular"
    id="<%= champ_fieldset_legend_id(@champ) %>"
  >
    <%= render EditableChamp::ChampLabelContentComponent.new(form: @form, champ: @champ, seen_at: @seen_at, row_number: @row_number) %>
  </legend>
  <% if @champ.description.present? %>
    <div class="fr-hint-text" id="<%= @champ.describedby_id %>">
      <%= render SimpleFormatComponent.new(@champ.description, allow_a: true) %>
    </div>
  <% end %>
<% end %>
