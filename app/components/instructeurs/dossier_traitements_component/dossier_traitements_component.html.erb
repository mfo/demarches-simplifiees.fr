<div class="tab-title"><%= t('.title') %></div>

<ul class="tab-list">
  <% traitements.each do |traitement| %>
    <li><%= t(".#{traitement.event}", **traitement_props(traitement)) %></li>
  <% end %>
</ul>
