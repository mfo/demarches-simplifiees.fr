<% content_for(:title, "Emails") %>
<% render_procedure_sticky_title(@procedure) %>

<%= render partial: 'shared/breadcrumbs_section',
  locals: { steps: [[admin_procedures_back_label(@procedure), admin_procedures_back_path(@procedure)],
                    [@procedure.libelle.truncate_words(10), admin_procedure_path(@procedure)],
                    ["Modèles d’email"]] } %>

<div class="fr-container">
  <div class="fr-grid-row fr-grid-row--gutters">
    <div class="fr-col-12">
      <h1 class="fr-h2">Modèles d’email</h1>

      <% if @procedure.accuse_lecture? %>
        <%= render Dsfr::AlertComponent.new(state: :info, size: :sm) do |c| %>
          <% c.with_body do %>
            <p>
              L’accusé de lecture est activé sur cette démarche. Dans ce
              contexte, les emails « d’acceptation », « de refus » et de «
              classement sans suite », ne sont pas modifiables afin de s’assurer
              que la décision finale reste masquée pour l’usager.
            </p>
          <% end %>
        <% end %>
      <% end %>
    </div>

    <% @mail_templates.each do |mail_template| %>
      <div class="fr-col-12">
        <%= render Procedure::EmailTemplateCardComponent.new(email_template: mail_template) %>
      </div>
    <% end %>
  </div>
</div>

<%= render Procedure::FixedFooterComponent.new(procedure: @procedure) %>
