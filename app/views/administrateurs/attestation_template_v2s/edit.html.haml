- content_for(:title, "Attestation")
= render partial: 'shared/breadcrumbs_section',
  locals: { steps: [[admin_procedures_back_label(@procedure), admin_procedures_back_path(@procedure)],
                    [@procedure.libelle.truncate_words(10), admin_procedure_path(@procedure)],
                    ['Attestation']] }

= render NestedForms::FormOwnerComponent.new

= render partial: "form", locals: { procedure: @procedure, attestation_template: @attestation_template, buttons: @buttons, attestation_kind: @attestation_template.kind }
