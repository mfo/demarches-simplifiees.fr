<% content_for(:title, t('views.users.dossiers.trash.title')) %>

<section class="dossiers-headers sub-header">
  <div class="fr-container fr-pb-3w">
    <p class="fr-mb-3w">
      <%= link_to t('views.users.dossiers.trash.back_to_list'), dossiers_path, class: 'fr-link fr-icon-arrow-left-line fr-link--icon-left' %>
    </p>

    <div class="mes-dossiers-header">
      <h1 class="fr-h3"><%= t('views.users.dossiers.trash.title') %></h1>
    </div>
  </div>
</section>

<div class="fr-container">
  <% if current_user.deleted_dossiers.present? %>
    <div class="trash-list-header">
      <%= link_to t('views.users.dossiers.trash.deleted_history_link'), deleted_dossiers_path, class: 'fr-link' %>
    </div>
  <% end %>

  <p class="mes-dossiers-results-count">
    <%= t('views.users.dossiers.trash.count', count: @total_count) %>
  </p>

  <%= render partial: 'dossiers_list', locals: { dossiers: @dossiers, filter: nil, search: false, statut: 'dossiers-supprimes' } %>
</div>
