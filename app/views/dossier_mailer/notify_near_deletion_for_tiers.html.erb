<% content_for(:title, "#{@subject}") %>

<p><%= t(:hello, scope: [:views, :shared, :greetings]) %></p>

<p>
  <%= t('.header_termine', count: @dossiers.size, expiration_date: human_date(@expiration_date.to_date)) %>

  <ul>
    <% @dossiers.each do |d| %>
      <li>
        <%= t('.list', count: @dossiers.size, dossier_id: d.id, libelle: d.procedure.libelle) %>

        <% if @dossiers.many? %>
          <%= d.mandataire_first_name %>
          <%= d.mandataire_last_name %>
          (
          <%= mail_to(d.user_email_for(:notification)) %>
          ).
        <% end %>
      </li>
    <% end %>
  </ul>
</p>

<p>
  <%= t('.more_information', count: @dossiers.size) %>

  <% if @dossiers.one? %>
    <% d = @dossiers.first %>
    <%= d.mandataire_first_name %>
    <%= d.mandataire_last_name %>
    (
    <%= mail_to(d.user_email_for(:notification)) %>
    ).
  <% end %>
</p>

<%= render partial: "layouts/mailers/signature" %>
